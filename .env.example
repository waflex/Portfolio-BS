# Desarrollo local (docker compose up -d en /backend)
PUBLIC_API_BASE=http://localhost:8000

# Producción (build estático)
# PUBLIC_API_BASE=https://internal.jrtdev.cl

# Web3Forms — ID de acceso (ya configurado en el host)
# Web3Forms_KEY=tu_key_aqui
